# Server
NODE_ENV=development
PORT=5000

# CORS - frontend origin(s), comma separated
CLIENT_ORIGIN=http://localhost:5173

# MongoDB
MONGO_URI=mongodb://127.0.0.1:27017/anjanaglobals

# JWT
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Email (optional - for lead notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Anjana Globals <no-reply@anjanaglobals.com>"
MAIL_TO=
